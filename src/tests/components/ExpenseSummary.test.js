import React from 'react';
import { shallow } from  'enzyme';
import ExpenseSummary from '../../components/ExpenseSummary';

test();

// test('should correctly render with 1 expense', () => {
// 	const wrapper = shallow(<ExpenseSummary expenseCount={1} expensesTotal={42069}/>);
// 	expect(wrapper).toMatchSnapshot();
// });

// test('should correctly render with many expense', () => {
// 	const wrapper = shallow(<ExpenseSummary expenseCount={42} expensesTotal={696969} />);
// 	expect(wrapper).toMatchSnapshot();
// });